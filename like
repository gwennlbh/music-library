#!/usr/bin/env bash
artist=$(echo $@ | cut -d/ -f1)
title=$(echo $@ | cut -d/ -f2)
echo Saving $artist — $title to library...
echo -e "$artist\t$title" >> $HOME/music/library.tsv
$HOME/music/backup
